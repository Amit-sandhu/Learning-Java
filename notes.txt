
basic syntax :

public class Main {
  public static void main(String[] args) {
    System.out.println("Hello World");
  }
}

static : to access the particular function you don't need any objects.
public : keyword defines the accessibilty of the function for jvm.
void : indicated that no output is returned by the main function and jvm doesn't expect a return value.
String[] args :  this is a string array that is passed to the main function. passed while running the code and compilation.


System.out.println(arr[1]);

System is class
out is object 
println is a method/function

Identifiers : the names given to define the variables, functions, classes, enum, anything.

The general rules for naming variables are :
    Names can contain letters, digits, underscores, and dollar signs
    Names must begin with a letter
    Names should start with a lowercase letter, and cannot contain whitespace
    Names can also begin with $ and _
    Names are case-sensitive ("myVar" and "myvar" are different variables)
    Reserved words (like Java keywords, such as int or boolean) cannot be used as names

java devs use camelcase convention for identifiers. (eg : camelCase, firstNum, thisIsNewIdentifier)
All constants identifiers are writen in caps. 

VARIABLES :

    Variables are containers for storing data values.

    In Java, there are different types of variables, for example:

    String - stores text, such as "Hello". String values are surrounded by double quotes
    int - stores integers (whole numbers), without decimals, such as 123 or -123
    float - stores floating point numbers, with decimals, such as 19.99 or -19.99
    char - stores single characters, such as 'a' or 'B'. Char values are surrounded by single quotes
    boolean - stores values with two states: true or false

there are two types of variables : primitive and non primitive:
primitive are base and predefined default data types by the language/ smallest unit of storage.(eg : int, boolean, double)
non-primitive are the derived datatypes. ( eg classes, arrays)

Float Data types:

1) float
2) Double

BOOLEAN : 

Implicit Typecasting : 

byte -> short -> int -> long -> float -> double

OPERATORS : 

Arithmetic operators : + - * / %
logical operators : && || !
&& and || are binary operands while ! is a unary operand as it only needs one operand.
conditional operators : < > = >= <= == !=

assignment operands = LHS value is stored in the RHS identifier.

NOTE : primitive data types start with lowercase letter while non primitive/derived data types start with an Uppercase letter.

method area contains code. stack heap.
when new objects are created they return their starting addresses.


==============================
JAVA MEMORY (Easy Explanation)
==============================

There are 3 main memory areas in Java:

1. STACK
- Stores method calls.
- Stores local variables.
- Stores references (addresses) to objects.
- Fast memory.
- Memory is automatically removed when a method finishes.

Example:
Student s = new Student();

Here, 's' is stored in the Stack.

--------------------------------------

2. HEAP
- Stores all objects created using 'new'.
- Stores instance (non-static) variables.
- Objects remain here until no references point to them.
- Garbage Collector removes unused objects.

Example:
Student s = new Student();

The actual Student object is stored in the Heap.

--------------------------------------

3. METHOD AREA (METASPACE)
- Stores class information.
- Stores method definitions.
- Stores constructor information.
- Stores static variables.

Example:
static String company = "Google";

'company' is stored once in the Method Area and shared by all objects.

--------------------------------------

Example:

class Student{
    static String company = "Google";
    String name;
    int age;
}

Student s = new Student();

Method Area:
- Student class
- company

Stack:
- reference variable 's'

Heap:
- Student object
- name
- age

--------------------------------------

Easy Analogy:

Method Area = University rulebook
(Defines what a Student is and stores shared static data.)

Heap = Hostel
(The actual students/objects live here.)

Stack = Reception Desk
(Keeps a list of room numbers (references) and temporary work.)

When you write:
Student s = new Student();

1. JVM reads the Student blueprint from the Method Area.
2. Creates a Student object in the Heap.
3. Stores the reference to that object in variable 's' on the Stack.

Remember:

Objects live in the HEAP.
References live in the STACK.
Static variables live in the METHOD AREA.

Think of a university.

Heap is like the hostel. Every student (object) actually lives there. 
When you write new Student(), you're building a new hostel room and putting a student in it.

Stack is like the reception desk. It doesn't contain the students—it only contains a 
list of room numbers (references) and temporary information needed while work is being done.

Method Area (Metaspace) is like the university rulebook. It contains the blueprint of what 
a Student is (its fields, methods, constructors) and shared information like static variables.
.


types of variables : 

LOCAL
GLOBAL
INSTANCE 
REFRENCE 
STATIC 


static and non static variables and functions and their accessibilty between each other :

static variables are created at the time class is created and remains in memeory area and can be accessed
by both static and non static functions

non-static variables are only created after object creation and can only be accessed by non-static function
directly and in static functions only after object creation.

this keyword : is a special reference variable by java that store the current the address of the object
or instance that calls the object variable while executing the code. 

Memory segmentation in JVM****************
Non-Primitive are only refrence type variables.

Arrays Random Access :

arrays have contiguous memory allocation.

Address of any index = Base Address + (size of datatype * index)

takes constant time to access any random index / element in an array as it finds the address of the particular
index and goes to that index in a constant time O(1).

int [][] arr = new int[3][];

above syntax doesn't give any errors even tho the 2nd size in 2d array is not desined. here the arr
is the refrence variable that is pointing to the arr[3] which is itself a simple array of references and 
the references are pointing to the 1d arrays of "yet not defined" arrays.

the 1d arrays can be intitalized using :
arr[0] = new int[5];
now arr[0] is a reference in the heap array (as array itself is stored in the heap and arr reference
variable is the only one stored in the stack) that is pointing to an int array of size 5 again in the heap mem.
